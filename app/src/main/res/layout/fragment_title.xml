<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.codeliner.achacha.titles.TitleViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/fragmentTitle_calendarContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/fragmentTitle_dayOfWeek"
                android:text="@{viewModel.date.dayOfWeek}"
                tools:text="목요일, "
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/fragmentTitle_day"
                app:layout_constraintTop_toTopOf="parent"/>

            <TextView
                android:id="@+id/fragmentTitle_day"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_s"
                tools:text="10일"
                app:layout_constraintTop_toTopOf="@id/fragmentTitle_dayOfWeek"
                app:layout_constraintStart_toEndOf="@+id/fragmentTitle_dayOfWeek"/>

            <TextView
                android:id="@+id/fragmentTitle_month"
                android:text="@{viewModel.date.month}"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xs"
                tools:text="December"
                app:layout_constraintTop_toBottomOf="@id/fragmentTitle_dayOfWeek"
                app:layout_constraintStart_toStartOf="@+id/fragmentTitle_dayOfWeek"/>

            <TextView
                android:id="@+id/fragmentTitle_taskCount"
                tools:text="12 Tasks"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toEndOf="@id/fragmentTitle_day"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintHorizontal_bias="1.0"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>